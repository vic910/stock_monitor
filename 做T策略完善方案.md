# 做T策略完善方案

> 目的：在不推倒现有框架的前提下，把「实时做T」信号从"以当日均价为中枢的高抛低吸"升级为**更准、更稳、能区分强弱势、能规避极端行情**的日内辅助信号。
> 桌面版（`compute_t_signal` / `compute_t_signal_v2`）与手机版（`mobile.html` 的 JS）逻辑一致，任何改动两端同步。

---

## 一、现状回顾

`compute_t_signal(price, ma10, intraday)`（v1）算法：

| 环节 | 公式 / 规则 |
|---|---|
| 中枢 | 当日均价 `avg = 成交额 ÷ 成交量` |
| 偏离 | `dev = (现价 − 均价) ÷ 均价` |
| 区间位置 | `pos = (现价 − 最低) ÷ (最高 − 最低)` |
| 自适应阈值 | `振幅 = (最高−最低)÷昨收`，`k = clamp(振幅×0.35, 0.5%, 3%)` |
| 低吸 | `(dev≤−k 或 pos≤20%)` 且 `现价>最低×1.001`（企稳） 且 非破位放量 |
| 高抛 | `(dev≥+k 或 pos≥80%)` 且 `现价<最高×0.999`（滞涨） |
| 破位放量否决 | `pos≤10% 且 量比≥2` → 不给低吸 |
| 参考价 | 买 `avg×(1−k)`、卖 `avg×(1+k)` |

**只在交易时段计算**，非盘中显示 `-`。

---

## 二、现有局限

1. **趋势没进硬判定**：`bullish (价≥MA10)` 在 v1 里只决定 tooltip 强度，不影响买卖 → 弱势股回踩照样报"低吸"，易抄在半山腰。
2. **看不清日内形态**：只用当日高低点+均价这几个标量，无法区分「V 型企稳回升」和「单边阴跌的短暂反抽」。
3. **量能只做否决、不做正向确认**。
4. **无极端行情保护**：接近涨停仍可能"高抛卖飞"、接近跌停仍可能"低吸接刀"。
5. **无环境过滤**：大盘跳水逆势低吸、大盘拉升过早高抛。
6. **阈值一套系数走天下**：`×0.35` 对 ETF/题材小盘/蓝筹/港股都一样。
7. **信号会抖**：每次刷新独立重算，价格在阈值边界来回时信号反复跳。
8. **港股保护更弱**：港股无量比字段，破位否决与量能项失效。

---

## 三、改进维度（按性价比排序）

### A 档 · 零新数据源（现有腾讯接口即可，性价比最高）
- **A1 趋势门控**：弱势(价<MA10)不给低吸、高抛权重加大 —— 改动最小、收益直接。
- **A2 BIAS 乖离率**：`BIAS_n=(现价−MA_n)÷MA_n`，超买压制低吸/增强高抛。
- **A3 涨跌停/极端涨幅安全阀**：涨幅≥+9% 高位持有、≤−9% 低位观望。
- **A4 量能正向确认**：低吸要缩量企稳、高抛要放量滞涨（不止 dump 否决）。
- **A5 开盘缺口/今开位置**：判断高开低走、低开高走的日内性质。
- **A6 信号冷却/去抖**：同区间只提示一次，穿越边界才更新。

### B 档 · 需分时/分钟 K 线（要加接口）
- **B1 分时均价线斜率**（区分强弱势）
- **B2 分钟布林带**（下轨低吸、上轨高抛，做T最核心工具）
- **B3 分钟 KDJ/RSI/MACD**
- **B4 盘中回撤动态止盈止损**

### C 档 · 环境择时过滤（提高胜率）
- **C1 大盘指数分时** · **C2 板块联动** · **C3 ATR 规范幅度**

---

## 四、推荐目标算法（整合设想）

把硬布尔升级为 **「环境门控 → 因子打分 → 强度分档 → 冷却输出」** 四层：

```
第0层 环境门控：涨跌停安全阀(A3) / 大盘跳水(C1) → 直接"观望/持有"
第1层 定位：avg / dev / pos / k（k 可按品种类型微调）
第2层 因子打分：低吸分 = f(dev/BIAS, pos, 缩量, 分时企稳, 趋势) − 破位放量惩罚
第3层 强度分档：弱/中/强（●/●●/●●●），对应操作力度
第4层 冷却输出(A6)：同区间保持，穿越边界/反向才更新
```

**好处**：单因子失效（如港股无量比）自动降权而非硬失效；强弱势、极端行情、环境都进决策；输出带强度。

---

## 五、分阶段落地

| 阶段 | 内容 | 数据 | 收益 |
|---|---|---|---|
| **Phase 1** ✅已实现 | A1 趋势门控 + A3 涨跌停安全阀 + A2 BIAS（新增「实时做T2」列，v1 保留对比；未含 A6 冷却） | 全现成 | 弱势不乱抄、极端不坑人 |
| Phase 2 | 打分制重构 + A4 量能确认 + A5 缺口 + A6 冷却 | 现成 | 信号分层、更稳 |
| Phase 3 | B1 分时斜率 + B2 分钟布林 | 加分钟K接口 | 专业化 |
| Phase 4 | C1 大盘 + C2 板块择时 | 加指数/板块接口 | 环境过滤 |

> **Phase 1 已落地**：`compute_t_signal_v2`，桌面新增「实时做T2」列、手机页每行 T2 徽章。阈值 涨跌停±9%、BIAS±6%（常量，可调）。

---

## 六、与现有代码的对接点

- **核心函数**：`compute_t_signal` (v1)、`compute_t_signal_v2` (v2)。后续因子多了可改为接收 `context` 字典。
- **两端一致**：桌面 Python 与手机 `mobile.html` 的 JS 是同一逻辑的两份实现，**每次改动必须同步**；阈值/权重建议集中成一份参数表，减少两端漂移。
- **新数据接口**：分钟K、大盘分时复用腾讯接口风格（`web.ifzq.gtimg.cn`）；手机端注意 CORS 与刷新频率。
- **状态存储**：信号冷却需少量状态——桌面存内存/`config`，手机存 `localStorage`（`data:` 页面不可用，需降级）。

---

## 七、原则与风险

1. **辅助非自动**：信号仅参考，不代替人工判断、不自动下单。
2. **灵敏度 vs 准确度**：过滤越多信号越少但越准；最好可配置。
3. **参数需校准**：`0.35`、`20%/80%`、`±9%`、`±6%`、各权重都应基于历史/实盘校准，建议后续接"做T信号回测"调参。
4. **数据风控**：分钟K/分时高频拉取会触发行情源限流；手机端还涉及流量与耗电。
5. **品种差异**：ETF、题材小盘、蓝筹、港股的合理做T幅度不同，参数最好按品种类型分档。
